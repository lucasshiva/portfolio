services:
  portfolio:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - GITHUB_USERNAME=${GITHUB_USERNAME:-lucasshiva}
      - CACHE_TTL_MINUTES=${CACHE_TTL_MINUTES:-5}
